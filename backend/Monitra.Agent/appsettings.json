{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "Agent": {
    "ApiBaseUrl": "https://localhost:5001",
    "HeartbeatIntervalSeconds": 60,

    // Only read on first run, when no device is registered yet. In a real MSI install this
    // is supplied by the installer (e.g. as an install-time parameter written here, or via
    // the MONITRA_INSTALL_TOKEN environment variable) rather than committed to this file.
    "InstallToken": ""
  }
}
